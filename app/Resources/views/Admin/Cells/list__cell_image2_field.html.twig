{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
    {% spaceless %}
        <div>
            {% if object.image2Name %}
                <img src="{{ vich_uploader_asset(object, 'image2File') | imagine_filter('60x60') }}" alt="thumbnail">
            {% else %}
                ---
            {% endif %}
        </div>
    {% endspaceless %}
{% endblock %}
